/* eslint-disable @typescript-eslint/no-unused-vars */
import { LogLevel, TelemetryEventName } from "./store/types";

export const registerGAPageview = (pageName: string): void => {
  return;
};

export const registerGAEvent = (eventName: string): void => {
  return;
};

export const logEvent = (logLevel: LogLevel, message: string): void => {
  return;
};

export const sendTelemetryEvent = (
  telemetryEventName: TelemetryEventName,
  message: string
): void => {
  return;
};
